{
  "name": "fantasy-route-mapper",
  "version": "1.7.1",
  "private": true,
  "description": "Lokale vanilla route mapper; Node.js is alleen nodig voor ontwikkelcontroles.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check": "node scripts/check.cjs",
    "test": "node tests/verify.cjs && node tests/flows.cjs",
    "build": "node scripts/release.cjs",
    "release": "node scripts/release.cjs"
  }
}
